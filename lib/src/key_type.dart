part of on_screen_keyboard;

/// Type for keyboard key.
enum KeyboardKeyType {
  /// Can be action key - Return, Backspace, etc.
  action,
  /// Keys that have text value - `Letters`, `Numbers`, `@` `.`
  string,
}
